{
  "name": "neo4j",
  "version": "1.22.10",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "build": "tsc --build",
    "watch": "yarn build --watch",
    "test": "yarn jest",
    "test:ci": "yarn jest --ci --coverage"
  },
  "devDependencies": {
    "@changesets/cli": "^2.12.0",
    "jest": "^26.6.3",
    "lerna": "^3.20.2",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
